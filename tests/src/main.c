// The error: Two Instructions are 8 bytes long instead of one instructions being 8 bytes long.

void _start(void) {
    // register int x = 1;
    // register int y = 1;
    // register int z = 1;
    return;
}
